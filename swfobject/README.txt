This module makes use of swfObject.

Download swfObject verison 2.1 from Google code (http://code.google.com/p/swfobject/) unzip it and replace use it to replace this folder.  We only make use of swfobject.js so that is the only file that is actually necessary.